// Mobile theme — re-exports the shared design tokens from @skillbridge/ui
// so screens import a single, consistent source of truth.
export {
  colors,
  typography,
  spacing,
  radius,
  shadow,
  theme,
} from '@skillbridge/ui';

export type { Theme } from '@skillbridge/ui';
