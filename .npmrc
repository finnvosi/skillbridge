# pnpm build-script approval — non-interactive.
# Allows Prisma's postinstall build scripts so `pnpm install` / `pnpm run build`
# don't abort on the ignored-builds gate.
enable-pre-post-scripts=true
